<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aquabill"><!-- Update with your actual package name -->

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- Replace with your app's name -->
        android:icon="@mipmap/ic_launcher" <!-- Replace with your app's icon -->
        android:usesCleartextTraffic="true">

    <!-- Ensure this points to MainActivity -->
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|direction|colorMode|uiMode|touchscreen|screenSize|density|fontSize|textSize"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
</manifest>

